% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circ_plot_metrics.R
\name{circ_plot_metrics}
\alias{circ_plot_metrics}
\title{Calculo das principais metricas e plots para parcelas circulares}
\usage{
circ_plot_metrics(
  spacing_x = NULL,
  spacing_y = NULL,
  n = NULL,
  methodology = "central",
  area = NULL,
  side_x = NULL,
  side_y = NULL,
  units = "ha",
  option = "inferior"
)
}
\arguments{
\item{spacing_x}{Espacamento entre as arvores na direcao X (em metros). Opcional se n for fornecido.}

\item{spacing_y}{Espacamento entre as arvores na direcao Y (em metros). Opcional se n for fornecido.}

\item{n}{Numero de arvores desejado. Opcional se area, side_x e side_y forem fornecidos.}

\item{methodology}{Metodologia a ser usada. Opcoes: "central", "vertical", "horizontal", "quadrante". Padrao e "central".}

\item{area}{Area da parcela. Pode ser fornecida em hectares ("ha") ou metros quadrados ("m2"). Opcional se side_x e side_y forem fornecidos.}

\item{side_x}{Comprimento do lado X da parcela (em metros). Opcional se area for fornecida.}

\item{side_y}{Comprimento do lado Y da parcela (em metros). Opcional se area for fornecida.}

\item{units}{Unidade de medida da area. Pode ser "ha" para hectares ou "m2" para metros quadrados. Padrao e "ha".}

\item{option}{Opcao de arredondamento para o numero de arvores. Pode ser "inferior" para arredondar para baixo, "superior" para arredondar para cima ou "exato" para manter o valor exato. Padrao e "inferior".}
}
\value{
Retorna uma lista com informacoes sobre o ponto central, raio, arvores dentro do circulo, contagem por linha e esquema de caminhamento.
}
\description{
Esta funcao executa todo o processo de calculo e plotagem das metricas de parcelas circulares de inventario de arvores.
}
\examples{
# Exemplo 1: Executar o processo com espacamento 2m x 2m em 1 ha, arredondando para baixo
circ_plot_metrics(spacing_x = 2, spacing_y = 2, area = 1, units = "ha", option = "inferior")

# Exemplo 2: Executar o processo com espacamento 3m x 3m em 5000 m2, arredondando para cima
circ_plot_metrics(spacing_x = 3, spacing_y = 3, area = 5000, units = "m2", option = "superior")

# Exemplo 3: Executar o processo a partir dos lados da parcela (100m x 50m) com espacamento 2m, arredondando para exato
circ_plot_metrics(spacing_x = 2, spacing_y = 2, side_x = 100, side_y = 50, units = "m2", option = "exato")

# Exemplo 4: Executar o processo fornecendo apenas o espacamento na direcao X (assume spacing_y = spacing_x)
circ_plot_metrics(spacing_x = 2.5, area = 2, units = "ha", option = "inferior")

}
